import "@testing-library/jest-dom";
//import { render, }
import userEvent from "@testing-library/user-event";
import { QuestionOption } from "./QuestionOption";

describe("QuestionOption", () => {
  it("should render question answers", () => {
    // given

    //const answers

    render(<QuestionOption />);
  });
});
